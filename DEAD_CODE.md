# Dead Code Report

Generated by automated analysis (knip, vulture, static reference tracing).

## Recently Cleaned (this session)

The following were confirmed dead and have been removed:

| File | Status |
|------|--------|
| `public/engines/rpg-topdown/style.css` | Removed — orphaned CSS, never linked by any HTML |
| `public/engines/rpg-topdown/assets/player.png` | Removed — unreferenced asset |
| `public/engines/rpg-topdown/assets/player_sheet.png` | Removed — unreferenced asset |
| `public/engines/rpg-topdown/assets/cave_bg.png` | Removed — unreferenced asset |
| `public/engines/rpg-topdown/assets/props/flag.png` | Removed — unreferenced asset |
| `public/engines/platformer-2d/tests/physics_unit_tests.html` | Removed — broken orphaned test page |
| `public/engines/platformer-2d/tests/slope_test.html` | Removed — broken orphaned test page |
| `public/engines/platformer-2d/tests/physics_test.html` | Removed — broken orphaned test page |
| `public/engines/platformer-2d/tests/jump_coyote_test.html` | Removed — broken orphaned test page |
| `server/utils/automationPolicy.js` — `normalizeAutomationPath` | Unexported — was only used internally and in tests |

## Previously Removed (prior sessions)

The files listed in earlier versions of this report (standalone migration scripts, unreferenced HTML pages, dead JS plugin modules, unused assets) had already been deleted from the repository prior to this session.

## Fixed Broken Features Found During Analysis

| Issue | Fix |
|-------|-----|
| `AdvancedGenerator.js` never loaded in `platformer_editor.html` | Added missing `<script>` tag alongside existing `SmartGenerator.js` include |

## Known Dead Exports Still Present

None confirmed. The following were previously flagged but are no longer dead:
- `safeFs.js` — `safeWriteFile`, `safeWriteFileSync`, `safeWriteFullPathSync` already removed from exports
- `ai-mode.mjs` — `AI_MODE_KEY` is a module-level const, not exported
- `automation-contract.mjs` — `KAP_VERSION` is a module-level const, not exported
- `editor-catalog.mjs` — `EDITOR_CATALOG` is a module-level const, not exported
- `automation-flags.mjs` — `AUTOMATION_FLAG_DEFAULTS` already renamed to non-exported `DEFAULTS`

## Summary

| Category | Count |
|----------|-------|
| Files removed this session | 9 |
| Dead exports cleaned this session | 1 |
| Broken script includes fixed | 1 |
| Archived items (removed prior) | ~24 |
