# Dead Code Report

Generated by automated analysis (knip, vulture, static reference tracing).

## A. Orphaned Files — Not Referenced Anywhere

These files have no detectable import, require, script tag, or build manifest reference.

### Standalone scripts (one-time migrations / dev tests)

| File | Notes |
|------|-------|
| `fix_c5.js` | One-time migration script |
| `fix_c6.js` | One-time migration script |
| `test_color.mjs` | Developer test for Three.js Color |
| `test_material.js` | Developer test for Three.js Material |
| `test_vsl_verification.js` | Developer test for VisualScriptEngine |
| `server/seed-dev-data.js` | Standalone data-seeding script |
| `public/dev-campaign-builder.js` | Unreferenced dev tool |

### Dead HTML entry points (no navigation link and no tool definition)

| File | Notes |
|------|-------|
| `login.html` | Auth page — unreferenced, no links |
| `reset-password.html` | Auth page — unreferenced, no links |
| `verify-email.html` | Auth page — unreferenced, no links |
| `audio_tool.html` | Integration is stubbed with only an alert — never navigated to |
| `database_editor.html` | Mentioned only in docs text, never linked |
| `debug-3d-dashboard.html` | No references |
| `example-integrated-editor.html` | No references |
| `ide.html` | Has a JS loader (`ide.js`) but no page links to it |
| `platformer_studio.html` | Superseded by `platformer_editor.html` — tool ID maps there |
| `search_tool.html` | Tool lookup (`openTool('search_tool')`) silently fails — no tool definition |
| `loading.html` | No references |

### Dead JS plugin modules (never included by any HTML)

| File | Notes |
|------|-------|
| `public/topdown3d_export_tools.js` | TopDown3D editor plugin — never loaded |
| `public/topdown3d_lighting_tools.js` | TopDown3D editor plugin — never loaded |
| `public/topdown3d_object_tools.js` | TopDown3D editor plugin — never loaded |
| `public/topdown3d_terrain_tools.js` | TopDown3D editor plugin — never loaded |

### Other dead assets

| File | Notes |
|------|-------|
| `public/sw.js` | Service worker — never registered (`navigator.serviceWorker.register` not found) |
| `public/css/kai-theme.css` | CSS file — linked by zero HTML pages |

---

## B. Unused Exports (within referenced files)

### `server/utils/safeFs.js` — 3 dead exports

| Function | Line | Status |
|----------|------|--------|
| `safeWriteFile` | 31 | **Dead** — never called anywhere |
| `safeWriteFileSync` | 45 | **Dead** — never called anywhere |
| `safeWriteFullPathSync` | 54 | **Dead** — never called anywhere |
| `isPathUnderRoot` | 68 | Exported but only used internally — could be un-exported |

Only `safeWriteFullPath` (async variant) is actively used by routes.

### `server/utils/automationPolicy.js` — 1 internal-only export

| Function | Line | Status |
|----------|------|-------|
| `normalizeAutomationPath` | 14 | Exported but only called internally and in test — no route imports it |

### `public/ai/ai-mode.mjs` — 1 dead export

| Export | Line | Status |
|--------|------|--------|
| `AI_MODE_KEY` | 1 | **Dead** — never imported by name. The literal string `'kai_ai_enabled'` is hardcoded at 5 call sites (`tool-registry.js`, `bridge.js`, `assistant-panel.js`, `ide.js`) |

### `public/ai/automation-contract.mjs` — 1 dead export

| Export | Line | Status |
|--------|------|--------|
| `KAP_VERSION` | 1 | **Dead** — only used internally at line 68, never imported by another module |

### `public/ai/editor-catalog.mjs` — 1 dead export

| Export | Line | Status |
|--------|------|--------|
| `EDITOR_CATALOG` | 1 | **Dead** — never imported by name. Only used internally by `editorForTool` (which is imported) |

### `public/ai/automation-flags.mjs` — 1 dead export

| Export | Line | Status |
|--------|------|--------|
| `AUTOMATION_FLAG_DEFAULTS` | 25 | **Dead** — never imported by name. `getAutomationFlags` is the active import |

---

## C. Dead Code in Backend (Python) — None Confirmed

Vulture flagged several items at 60% confidence, but every flagged function is either:
- A FastAPI route with an `@app` decorator (`get_metrics`, `get_persona`, `update_persona`, `save_history`, `load_history`)
- Called internally (`ingest_project` called from `main.py`, `ingest_file` called from `watcher.py`)
- A watchdog callback (`on_modified`, `on_created`) invoked by the watchdog library
- A class attribute accessed via `self`/prototype from other methods

**No reliably dead code found in the Python backend.**

---

## D. Unused Studio-UI TypeScript Components

The `studio-ui/src/components/` directory contains React components and `studio-ui/src/main_*.tsx` entry points that are built by Vite. These are entry-point-driven: each `main_*.tsx` is the entry for a specific Vite HTML page. Since knip reports them as "unused files" (not directly imported by other ES modules), they should be verified against the Vite build configuration to confirm they are still wired as entry points.

**Note:** These are likely false positives — Vite HTML entry points in `studio-ui/` each import their respective `main_*.tsx`. If those HTML files are generated or in a non-standard location, knip may miss them.

---

## Summary

| Category | Count |
|----------|-------|
| Orphaned standalone scripts | 6 |
| Unreferenced HTML pages | 11 |
| Dead JS plugin modules | 4 |
| Other dead assets | 2 |
| Dead exports (server utils) | 4 |
| Dead exports (AI modules) | 4 |

**Total: ~31 items** worth investigating for removal.
